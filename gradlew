#!/bin/sh

# Placeholder wrapper script.
# Open this project in Android Studio to regenerate/upgrade the Gradle wrapper if needed.
# Typical command after wrapper generation:
#   ./gradlew assembleDebug

echo "Gradle wrapper is not bundled in this environment. Open the project in Android Studio and run: gradle wrapper or let Studio sync it."
exit 1
