package com.cvuong233.cinephantom.widget

import android.appwidget.AppWidgetProvider

/**
 * DIAGNOSTIC: Empty provider. No onUpdate, no setText, no RemoteViews.
 * ONLY the initial layout (widget_imdb_search_big.xml) will appear.
 * If the user sees BRIGHT GREEN with "BIG WIDGET" text, we know the
 * widget registration and layout inflation are working correctly.
 */
class ImdbSearchWidgetBigProvider : AppWidgetProvider()
